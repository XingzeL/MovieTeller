# media_utils

Shared media probing helpers for MovieTeller.

Current scope:

- resolve sibling `ffprobe` from an `ffmpeg` path
- probe media/container duration
- compute effective segment duration from optional time bounds
