# frame_source

Unified frame sourcing for MovieTeller narration.

Current scope:

- uniform frame extraction from a local video segment
- frame-pool backed frame retrieval
- normalized `FrameBatch` output for downstream narration
