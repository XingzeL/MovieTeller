# rerank

Reusable reranking utilities for MovieTeller.

Phase 1 scope:

- generic Maximal Marginal Relevance (MMR) selection
- query-aware reranking across arbitrary candidate embeddings
- no subtitle- or frame-specific business logic
