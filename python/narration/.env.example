# 旁白模块运行时依赖 movieteller_config 解析后的键名。
# 复制仓库根目录 .env.example 为 .env，并按 README 填写。
# 本文件仅作交叉引用提示；权威列表见 ../../.env.example

OPENAI_API_KEY=
# NARRATION_PROVIDER=openai
# NARRATION_IMAGE_MODEL=gpt-4o-mini
# MAX_FRAMES_PER_SEGMENT=24
# NARRATION_FRAME_MAX_EDGE=768
# FFMPEG_PATH=ffmpeg
